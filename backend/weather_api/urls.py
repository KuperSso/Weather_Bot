# from django.urls import path
# from .views import LogList, UserLogList

# urlpatterns = [
#     path('logs/', LogList.as_view(), name='log-list'),
#     path('logs/<int:user_id>/', UserLogList.as_view(), name='user-log-list'),
# ]